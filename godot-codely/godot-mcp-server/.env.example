# 复制为 .env 并填入你的火山方舟（Volcengine Ark）API Key。
# .env 已被 .gitignore 排除，不会提交泄漏。image-gen 工具（generate_sprite/generate_image）会读取。
VOLCENGINE_API_KEY=your_ark_api_key_here

# 可选：接入点/模型覆盖。
# 默认使用仓库内 image-gen 硬编码的作者账号 ep- 接入点（ep-20260320081042-cz2rc），
# 只有作者账号的 key 能出图。别人用自己的 key 时建议设自己的接入点（ep-xxx）或模型名：
# VOLC_IMAGE_ENDPOINT=ep-xxxxxxxx
# VOLC_IMAGE_MODEL=doubao-seedream-x
